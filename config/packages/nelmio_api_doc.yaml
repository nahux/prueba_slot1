nelmio_api_doc:
    documentation:
        info:
            title: Prueba Slot1
            description: Web Service que retorna la información del clima de una ciudad
            version: 1.0.0
    routes: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
